import styled from "styled-components";

const HeaderStyles = styled.div`
  text-align: center;
`;

export function Header() {
  return (
    <HeaderStyles>
      <h1>Rimil's Library</h1>
    </HeaderStyles>
  );
}
